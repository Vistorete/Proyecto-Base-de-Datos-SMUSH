{{!Archivo para listar nuestros enlaces}}
<div class="container p-4">
    <div class="row">

        {{#each links}} {{!Sirve para recorrer las listas}}
            <div class="col-md-3"> {{!Columna de 3}}
                <div class="card text-center"> {{!Cartas con el contenido centrado}}
                    <div class="card-body"> {{!Cuerpo para mostrar el contenido de la tarjeta}}
                        <a href="{{url}}" target=".blank"> {{!Redirecciona a la URl creada}}
                            <h3 class="card-title text-uppercase"> {{!Título de la tarjeta en mayúscula}}
                                {{title}}
                            </h3>
                        </a> 
                        <p class="m-2">{{description}}</p> {{!Descripción de cada enlace guardado}}
                        <p>{{timeago created_at}}</p> {{!Método timeago que recibe el parámetro created_at (momento en que se ingresaron los datos)}}
                        <a href="/links/delete/{{id}}" class="btn btn-danger">Delete link</a> {{!Borra el link guardado con el botón "Delete link"}}
                        <a href="/links/edit/{{id}}" class="btn btn-secondary">Edit</a>
                    </div>
                </div>
            </div>
        {{else}}    {{!Si no hay links crea}}
            <div class="col-md-4 mx-auto">
                <div class="card card-body text-center">
                    <p>There are not links created yet.</p>
                    <a href="/links/add">Create One!</a> {{!Redirecciona a links/add para agregar un usuario}}
                </div>
            </div>
        {{/each}}
    </div>
</div> 